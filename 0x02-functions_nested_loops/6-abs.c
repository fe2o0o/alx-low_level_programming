#include "main.h"
#include <stdlib.h>
/**
 * _abs - to computes the absolute value of an integer.
 * @n : the int to check
 * Return: the absolute value of n
 */
int _abs(int n)
{
	int res = abs(n);

	return (res);
}
